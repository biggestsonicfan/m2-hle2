name: canary

# Rolling canary: every push to master builds, tests, and republishes a single
# pre-release tagged `canary`. Download URLs are therefore stable:
#   https://github.com/<owner>/m2-hle2/releases/download/canary/m2hle-windows-x64.zip
on:
  push:
    branches: [master]
    # tools/ is the Node grading harness — it never changes the binary.
    # tools/ is the Node grading harness -- it never changes the binary.
    # packaging/rocknix/*.md is in the ARC-S zip, so it is not ignored there.
    paths-ignore: ['*.md', 'tools/**']
  workflow_dispatch:
  # schedule:
  #   - cron: '0 7 * * *'   # uncomment for a nightly build even without pushes

# One canary at a time; a newer push supersedes an in-flight build.
concurrency:
  group: canary
  cancel-in-progress: true

permissions:
  contents: write   # needed to move the tag and publish the release

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            name: windows-x64
            exe: m2hle.exe
          - os: ubuntu-latest
            name: linux-x64
            exe: m2hle
            # Linux/X11 path is unproven; don't let it block the Windows canary.
            experimental: true
    runs-on: ${{ matrix.os }}
    continue-on-error: ${{ matrix.experimental || false }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0        # git describe / commit count for the version stamp
          submodules: false     # vendor/noclip is big and only feeds tools/

      # Everything the CMake build needs, minus vendor/noclip.
      #
      # This list has to track CMakeLists.txt's M2HLE_SUBMODULE_PATHS, and it did
      # not when vendor/imgui_club arrived with the memory editor. Nothing caught
      # it because this workflow only runs on a push to master: a submodule added
      # in a PR is first exercised by the build that follows its own merge, so the
      # break lands on master rather than on the branch that caused it.
      - name: Fetch build submodules
        run: >
          git submodule update --init
          vendor/imgui vendor/dear_bindings vendor/sokol vendor/miniz vendor/ImGuiFileDialog
          vendor/imgui_club

      # cimgui is generated from vendor/imgui by vendor/dear_bindings at build time.
      # One pinned interpreter for both pip and CMake: left to itself,
      # find_package(Python3) picks whichever Python the runner image ships
      # newest (python3.exe 3.14 on windows-latest), which is not the one
      # `python -m pip` installed ply into, and configure fails.
      - uses: actions/setup-python@v7
        id: py
        with:
          python-version: '3.12'

      - name: Install dear_bindings dependencies
        shell: bash
        run: '"${{ steps.py.outputs.python-path }}" -m pip install ply==3.11'

      - name: Install Linux deps
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libx11-dev libxi-dev libxcursor-dev libgl1-mesa-dev libasound2-dev

      - name: Stamp version
        id: ver
        shell: bash
        run: |
          echo "version=r$(git rev-list --count HEAD)-$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      - name: Configure
        run: >
          cmake -S . -B build
          -DCMAKE_BUILD_TYPE=Release
          -DPython3_EXECUTABLE="${{ steps.py.outputs.python-path }}"
          -DM2HLE_VERSION="${{ steps.ver.outputs.version }}"

      - name: Build
        run: cmake --build build --config Release -j 4

      # rom_test / boot_test / geo_test / input_test hardcode a dev-machine ROM
      # path and cannot run here. The rest are self-contained.
      - name: Test
        run: >
          ctest --test-dir build -C Release --output-on-failure
          -R "^(mem_test|i960_test|cop_test|m68k_test|emu_test)$"

      - name: Package
        shell: bash
        run: |
          mkdir -p dist/stage
          cp build/Release/${{ matrix.exe }} dist/stage/
          cp README.md dist/stage/
          echo "${{ steps.ver.outputs.version }} (${{ github.sha }})" > dist/stage/VERSION.txt
          cd dist/stage
          if [ "$RUNNER_OS" = "Windows" ]; then
            7z a ../m2hle-${{ matrix.name }}.zip ./*
          else
            zip -r ../m2hle-${{ matrix.name }}.zip ./*
          fi

      - uses: actions/upload-artifact@v7
        with:
          name: m2hle-${{ matrix.name }}
          path: dist/*.zip
          retention-days: 14   # per-commit history, independent of the rolling release


  # The handheld build: the sdl3 frontend (src/main_sdl.c) cross-compiled for the
  # RK3566, for ROCKNIX on the Anbernic RG ARC-S. packaging/rocknix/README.md is
  # the install side; this job is why there is a download to install.
  #
  # debian:trixie, not the runner's Ubuntu, because trixie is the distribution
  # that has libsdl3-dev for arm64 AND a glibc (2.41) that matches ROCKNIX's, so
  # the binary links against the device's own libSDL3 / libGLESv2 / libc and
  # carries none of them. Cross-compiled rather than built on an arm64 runner for
  # the same reason: what matters is the sysroot, not the machine.
  rocknix:
    runs-on: ubuntu-latest
    container: debian:trixie
    steps:
      - name: Install the cross toolchain and the arm64 sysroot
        run: |
          dpkg --add-architecture arm64
          apt-get update -qq
          DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
            crossbuild-essential-arm64 cmake ninja-build git ca-certificates file zip \
            libsdl3-dev:arm64 libgles-dev:arm64 libegl-dev:arm64

      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          submodules: false

      # The sdl3 frontend's whole submodule list (CMakeLists.txt
      # M2HLE_SUBMODULE_PATHS on the M2HLE_SDL3 path): no ImGui, so no
      # dear_bindings and no Python.
      - name: Fetch build submodules
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git submodule update --init vendor/sokol vendor/miniz

      - name: Stamp version
        id: ver
        run: echo "version=r$(git rev-list --count HEAD)-$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      # CMAKE_TOOLCHAIN_FILE must be absolute: a relative one is looked for
      # from the BUILD directory, so "packaging/rocknix/aarch64.cmake" is not
      # found and the configure silently falls back to the host compiler.
      - name: Configure
        run: >
          cmake -S . -B build -G Ninja
          -DCMAKE_BUILD_TYPE=Release
          -DM2HLE_FRONTEND=sdl3
          -DM2HLE_VERSION="${{ steps.ver.outputs.version }}"
          -DCMAKE_TOOLCHAIN_FILE="$GITHUB_WORKSPACE/packaging/rocknix/aarch64.cmake"

      - name: Build
        run: cmake --build build -j 4

      # A cross build cannot be run here, so check what can be checked without
      # running it: that it is an aarch64 ELF, and that it asks for exactly the
      # four libraries ROCKNIX has. A fifth NEEDED entry is a dependency the
      # device may not carry, and is worth failing the build over.
      - name: Check the binary
        run: |
          aarch64-linux-gnu-strip build/m2hle
          file build/m2hle | grep -q 'ELF 64-bit LSB .*ARM aarch64' || { file build/m2hle; exit 1; }
          aarch64-linux-gnu-objdump -p build/m2hle | awk '/NEEDED/ { print $2 }' | sort > /tmp/needed
          cat /tmp/needed
          for so in libGLESv2.so.2 libSDL3.so.0 libc.so.6 libm.so.6; do echo "$so"; done > /tmp/want
          diff /tmp/want /tmp/needed

      - name: Package
        run: |
          mkdir -p dist/stage
          cp build/m2hle dist/stage/
          cp packaging/rocknix/start_m2hle.sh packaging/rocknix/install-es.sh dist/stage/
          cp packaging/rocknix/README.md dist/stage/
          chmod +x dist/stage/m2hle dist/stage/*.sh
          echo "${{ steps.ver.outputs.version }} (${{ github.sha }})" > dist/stage/VERSION.txt
          cd dist/stage && zip -r ../m2hle-rocknix-arm64.zip ./*

      - uses: actions/upload-artifact@v7
        with:
          name: m2hle-rocknix-arm64
          path: dist/*.zip
          retention-days: 14

  publish:
    needs: [build, rocknix]
    # A handheld build that fails must not hold back the Windows canary.
    # It does leave the previous m2hle-rocknix-arm64.zip on the release, so
    # the notes below say which archives this run actually produced.
    if: always() && github.event_name != 'pull_request' && needs.build.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with: { fetch-depth: 0, submodules: false }

      - uses: actions/download-artifact@v8
        with:
          path: dl
          pattern: m2hle-*
          merge-multiple: true

      - name: Move the canary tag to this commit
        run: |
          git tag -f canary "$GITHUB_SHA"
          git push -f origin refs/tags/canary

      - name: Publish / replace the rolling release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          NOTES="Rolling canary build of \`master\`.

          Commit: ${GITHUB_SHA}
          Built: $(date -u '+%Y-%m-%d %H:%M UTC')

          $(git log -5 --pretty='- %s (%h)')

          Unsigned development build. No ROMs are included — supply your own.

          Built this run:
          $(cd dl && ls -1 *.zip | sed 's/^/- /')"
          if gh release view canary >/dev/null 2>&1; then
            gh release edit canary --notes "$NOTES" --prerelease --latest=false
          else
            gh release create canary --title "Canary (master)" --notes "$NOTES" \
              --prerelease --latest=false
          fi
          gh release upload canary dl/*.zip --clobber
