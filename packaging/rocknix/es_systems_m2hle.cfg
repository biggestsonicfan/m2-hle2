<?xml version="1.0"?>
<!-- Sega Model 2 for EmulationStation, as a drop-in.

     ES merges every /storage/.emulationstation/es_systems_*.cfg into the system
     list, and ROCKNIX's /usr/share/post-update only ever moves es_systems.cfg
     and es_features.cfg aside (to last_*.cfg) and symlinks the read-only
     /usr/config copies over them, "so they are managed with OS updates". So an
     edit to es_systems.cfg lasts until the next OS upgrade and a file with this
     name lasts indefinitely. install-es.sh writes it; change it there.

     ROCKNIX 7.0.2 (20260919) removed the segamodel2 system and the sm2-emu
     package, so this defines the whole system rather than adding an emulator to
     one the OS ships. The name is still segamodel2: it is what /storage/roms,
     gamelist.xml, the scraped media and every per-game setting are already
     keyed on, and what the art-book-next theme draws its AM2 tile from.

     <command> goes through m2hle-runemu.sh rather than /usr/bin/runemu.sh
     because runemu.sh looks for a standalone emulator's launcher at
     /usr/bin/start_<core>.sh and /usr is a read-only squashfs. The wrapper
     re-derives a patched copy from whatever runemu.sh the OS currently ships,
     so an upgrade cannot leave us on a stale fork of it. -->
<systemList>
	<system>
		<name>segamodel2</name>
		<fullname>Sega Model 2</fullname>
		<manufacturer>Sega</manufacturer>
		<release>1993</release>
		<hardware>arcade</hardware>
		<path>/storage/roms/segamodel2</path>
		<!-- .sh so "Update m2-hle.sh" is listed beside the games; runemu.sh runs
		     a rom whose name ends in .sh directly, whatever the core is. -->
		<extension>.zip .7z .sh</extension>
		<command>/storage/.local/bin/m2hle-runemu.sh %ROM% -P%SYSTEM% --core=%CORE% --emulator=%EMULATOR% --controllers="%CONTROLLERSCONFIG%"</command>
		<platform>arcade</platform>
		<theme>model2</theme>
		<emulators>
			<emulator name="retroarch">
				<cores>
					<core default="true">m2hle</core>
				</cores>
			</emulator>
			<emulator name="m2hle">
				<cores>
					<core>m2hle-sa</core>
				</cores>
			</emulator>
		</emulators>
	</system>
</systemList>
