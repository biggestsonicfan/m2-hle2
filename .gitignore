# Build output
build/
build_vs22/
out/
bin/
*.exe
*.obj
*.o
*.a
*.lib
*.pdb
*.ilk
*.exp

# CMake
CMakeCache.txt
CMakeFiles/
CMakeScripts/
cmake_install.cmake
install_manifest.txt
compile_commands.json
.cmake/

# Visual Studio / VS Code
.vs/
.vscode/
*.user
*.suo
*.sln.docstates
CMakeSettings.json.user

# Emscripten
*.wasm
*.js.mem

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# ROMs / proprietary content (never commit)
*.zip
roms/
sfight/
schamp/
epr-*.bin
epr-*.15
epr-*.16
mpr-*.bin

# Reference assets (large, external)
3d-reference/
*.obj

# Vendored deps tarballs / archives
vendor/*.tar.gz
vendor/*.zip

# Local notes
NOTES.local.md
TODO.local.md

# Runtime artifacts
m2hle.log
*.log

# Python (mcp_server)
__pycache__/
*.pyc
mcp_server/.venv/

# Homebrew / ROM staging used for local m2snake testing (contains ROM images)
test_m2snake/

# Runtime debug dumps written by geo3d.h / the camera logger
cam_ours.csv
model_tex.txt
model_uv.txt

# mcp_server/ holds one real server + client; everything else there is
# throwaway diagnostic scripts and screenshots. Whitelist the real files.
mcp_server/*
!mcp_server/server.py
!mcp_server/m2hle_client.py
!mcp_server/requirements.txt

# Grading harness output — captures of the running game (its own data) and the
# model dumps. tools/ defaults to a temp directory outside the checkout; these
# cover a run that was pointed here instead.
tools/out/
tools/**/capture.json
texram0.bin
texram1.bin
lumaram.bin
colorxlat.bin
palram.bin
build_vs22_*.log
